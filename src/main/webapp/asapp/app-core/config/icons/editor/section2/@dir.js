define([ 'text!./blockquote.svg', 'text!./column.svg', 'text!./fragment.svg', 'text!./row.svg', 'text!./section.svg' ], function(blockquote, column, fragment, row, section) {
	return {
		blockquote : blockquote,
		column : column,
		fragment : fragment,
		row : row,
		section : section
	};
});